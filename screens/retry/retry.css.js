import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  error__text: {
    fontSize: 15,
    marginBottom: 5,
  },
  button: {
    marginTop: 15,
  },
  retry: {
    fontSize: 16,
    color: '#fff',
  },
});
