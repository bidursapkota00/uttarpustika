import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  surface: {
    flex: 1,
    height: '100%',
    width: '100%',
    resizeMode: 'contain',
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'row',
  },
  formc: {
    width: '100%',
    padding: '10%',
  },
  text: {
    textAlign: 'center',
    marginBottom: 20,
  },
  input: {
    marginBottom: 15,
  },
});
