import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 25,
    paddingTop: 50,
  },
  title: {
    textAlign: 'center',
    marginBottom: 20,
  },
  input: {
    marginBottom: 15,
  },
  snackbar: {
    flex: 1,
    width: '100%',
    alignItems: 'center',
  },
});
